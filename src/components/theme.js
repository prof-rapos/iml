// Shared text colors for the GitHub-dark-styled views (IDE, Behavioural
// Modeling, Model Transformations) — identical in every file that declared
// them locally. Borders/accents/backgrounds are deliberately NOT unified
// here: they vary intentionally per module (e.g. accent color distinguishes
// Behavioural's amber/purple from the IDE's blue).
export const TEXT     = '#e6edf3';
export const TEXT_DIM = '#8b949e';
